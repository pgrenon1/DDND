using UnityEngine;

public class Note : MonoBehaviour
{
    private Vector3 _origin;
    private Vector3 _targetPosition;


    //public float Beat { get; set; }
    //public float BeatsShownInAdvance { get; set; }

    //private void Update()
    //{
    //    transform.position = Vector2.Lerp(_origin, _targetPosition, (Conductor.Instance.songBpm - (Beat - Conductor.Instance.songPositionInBeats)) / BeatsShownInAdvance);
    //}
}
